<p id="notice"><%= notice %></p>

<h1>Livres</h1>

<table>
  <thead>
    <tr>
      <th>Titre</th>
      <th>Nb pages</th>
      <th>Isbn</th>
      <th>Nb consultation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @livres.each do |livre| %>
      <tr>
        <td><%= livre.titre %></td>
        <td><%= livre.nb_pages %></td>
        <td><%= livre.isbn %></td>
        <td><%= livre.nb_consultation %></td>
        <td><%= link_to 'Show', livre %></td>
        <td><%= link_to 'Edit', edit_livre_path(livre) %></td>
        <td><%= link_to 'Destroy', livre, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Livre', new_livre_path %>
