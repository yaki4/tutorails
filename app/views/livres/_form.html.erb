<%= form_with(model: livre, local: true) do |form| %>
  <% if livre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(livre.errors.count, "error") %> prohibited this livre from being saved:</h2>

      <ul>
        <% livre.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titre %>
    <%= form.text_field :titre %>
  </div>

  <div class="field">
    <%= form.label :nb_pages %>
    <%= form.text_field :nb_pages %>
  </div>

  <div class="field">
    <%= form.label :isbn %>
    <%= form.text_field :isbn %>
  </div>

  <div class="field">
    <%= form.label :nb_consultation %>
    <%= form.text_field :nb_consultation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
